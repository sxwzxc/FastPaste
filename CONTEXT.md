# FastPaste

跨平台静默剪贴板管理器，常驻托盘、监听剪贴板、通过全局热键将最近 10 条纯文本条目直接粘贴到焦点窗口。

## Language

### 历史与条目

**条目**:
用户一次复制产生的纯文本片段（UTF-8），超过 5MB 的文本不视为条目。
_Avoid_: 消息、剪贴内容、片段

**历史**:
容量为 10 的环形队列，按复制时间倒序排列，`1` 为最新，`0` 为最旧，去重后置顶，空或纯空白不入队。
_Avoid_: 列表、记录、缓存

**敏感内容**:
被系统标记为瞬态（transient）或匹配 `ignore_regex` 的文本，不进入历史。
_Avoid_: 密码、私密文本

### 状态与行为

**启用状态**:
同时开启剪贴板监听与全局热键响应，历史保留。
_Avoid_: 开启、运行中

**停用状态**:
同时暂停监听与禁用热键，历史保留，停用期复制不补录。
_Avoid_: 关闭、暂停

**粘贴**:
将指定条目写入当前焦点窗口的输入焦点。可由击键注入或剪贴板粘贴完成，对应槽位无条目时静默忽略，不提示。
_Avoid_: 复制、键入、注入

**击键注入**:
不经系统剪贴板、以按键事件把条目送入焦点的粘贴方式。
_Avoid_: type、模拟输入、键入

**剪贴板粘贴**:
经系统剪贴板与粘贴快捷键把条目送入焦点的粘贴方式。
_Avoid_: clipboard 方式、Ctrl+V

**粘贴方式**:
一次粘贴所选用的路径，为击键注入或剪贴板粘贴。
_Avoid_: 后端、backend

**自动选择**:
不含换行、Tab 等 C0 控制字符、且不超过 1000 个 Unicode 标量的条目走击键注入，否则走剪贴板粘贴。
_Avoid_: auto 后端、混合粘贴

**保留剪贴板**:
仅在剪贴板粘贴时，是否在完成后把用户原剪贴板内容恢复回去。
_Avoid_: 不污染剪贴板、还原

### 配置与交互

**配置**:
持久化于系统配置目录的 `config.toml`，定义热键、保留剪贴板、粘贴方式、自启等选项，格式为 `ctrl+shift+1`。
_Avoid_: 设置、偏好

**重载配置**:
重新读取并校验配置文件，成功则整体应用，任一环节失败则整体保留旧配置不应用。
_Avoid_: 刷新、热重载

**手动重载**:
经托盘菜单触发的重载配置，成败均以系统对话框即时反馈。

**自动重载**:
检测到配置文件变更后触发的重载配置，内容未变化时不重复应用；加载或校验失败时保留旧配置且不作任何提示。
_Avoid_: 热重载、watch

**托盘**:
应用唯一常驻界面，承载启用/停用、历史预览、打开配置、重载配置、自启开关、权限诊断与退出。
_Avoid_: 任务栏、状态栏、菜单栏

**自启**:
通过系统自启机制在用户登录时自动拉起应用，Windows 下为 Task Scheduler 提权任务，macOS 下为 LaunchAgent/Daemon。
_Avoid_: 开机启动、自动运行

**权限诊断**:
托盘提供的诊断入口，用于引导用户授予 Accessibility（macOS）或处理 UAC/提权失败。
_Avoid_: 权限检查、诊断
