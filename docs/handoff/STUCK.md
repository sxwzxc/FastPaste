刚才这一步卡住了。不要开始下一步，也不要重构无关代码。

1. 复述当前步骤编号和「目标」一句。
2. 贴出编译器或 `cargo test` 的**第一条**报错（含文件路径和行号）。
3. 只修这一条报错，再跑 `cargo test`。
4. 若接口猜错（例如 muda 没有 `set_text`）：打开本机 cargo registry 里对应 crate 的源码或 docs.rs，按真实方法名改，不要臆造。
5. 修到该步「Done when」全 yes 为止。仍不行就列出你已尝试的 3 件事和你认为的阻塞点，然后停。
